# Распределённый игровой бэкенд

> [🇬🇧 English version](README.md)

Продакшн-система распределённой игры с **AWS Lambda микробэкендами** для трансляции тиков в реальном времени, **Cloudflare Workers** edge gateway и **Apache Cassandra/DynamoDB** для хранения данных.

## 🎮 Ключевые возможности

- **Трансляция тиков в реальном времени**: AWS Lambda микробэкенды транслируют игровые тики игрокам через WebSocket
- **Мультирегиональное развёртывание**: Развёртывание в 8+ регионах AWS для минимальной задержки
- **Контроль задержки**: Автоматическое отключение игроков при превышении порогов задержки
- **Детерминированный игровой движок**: Идентичное вычисление состояния во всех регионах
- **Edge Gateway**: Cloudflare Workers для интеллектуальной маршрутизации
- **Масштабируемое хранилище**: DynamoDB для Lambda, Cassandra для основного бэкенда

## 📐 Обзор архитектуры

```
┌─────────────┐     ┌─────────────┐     ┌─────────────────────────┐
│   Игрок     │────▶│  Edge GW    │────▶│  Tick Broadcaster       │
│  (Браузер)  │     │ (Cloudflare)│     │  (AWS Lambda)           │
└─────────────┘     └─────────────┘     └─────────────────────────┘
      ▲                                           │
      │       WebSocket (тики каждые 100мс)       │
      └───────────────────────────────────────────┘
```

**Полная архитектура**: [ARCHITECTURE.ru.md](ARCHITECTURE.ru.md)

## 📂 Структура проекта

```
.
├── tick-broadcaster/       # AWS Lambda микробэкенд (НОВОЕ)
│   ├── src/
│   │   ├── handlers/       # WebSocket, HTTP, tick обработчики
│   │   ├── engine.ts       # Детерминированный игровой движок
│   │   └── utils/          # Утилиты задержки, WebSocket
│   └── serverless.yml      # Конфигурация Serverless Framework
├── frontend/               # React приложение с WebSocket клиентом
│   └── src/
│       ├── App.tsx         # Главный компонент
│       └── utils/
│           ├── api.ts      # API клиент
│           └── websocket.ts # WebSocket клиент
├── edge/                   # Cloudflare Workers gateway
│   └── src/
│       ├── handlers.ts     # Обработчики запросов
│       └── config.ts       # Конфигурация регионов
├── deterministic-backend/  # Go бэкенд (устаревший/опционально)
├── cmd/server/             # Go main server
└── internal/               # Go internal packages
```

## 🚀 Быстрый старт

### 1. Развёртывание Tick Broadcaster (AWS Lambda)

```bash
cd tick-broadcaster
npm install
npm run build

# Развернуть во всех регионах
npm run deploy:all

# Или развернуть в конкретном регионе
npm run deploy:eu      # EU (Ирландия)
npm run deploy:us      # US East (Северная Вирджиния)
npm run deploy:asia    # Asia (Токио)
```

### 2. Развёртывание Edge Gateway (Cloudflare Workers)

```bash
cd edge
npm install

# Обновить wrangler.toml с эндпоинтами tick-broadcaster
npm run deploy
```

### 3. Запуск Frontend

```bash
cd frontend
npm install

# Установить переменные окружения
export REACT_APP_TICK_EU_WS=wss://xxx.execute-api.eu-west-1.amazonaws.com/prod
export REACT_APP_TICK_US_WS=wss://xxx.execute-api.us-east-1.amazonaws.com/prod

npm start
```

## ⚡ Пороговые значения задержки

Система контролирует пороговые значения задержки для честной игры:

| Порог | По умолчанию | Описание |
|-------|--------------|----------|
| Макс. задержка | 150мс | Игрок отключается при превышении RTT |
| Макс. джиттер | 50мс | Игрок отключается при нестабильности |
| Предупр. задержка | 100мс | Игрок получает предупреждение |
| Предупр. джиттер | 30мс | Игрок получает предупреждение |

## 🌍 Поддерживаемые регионы

| Регион | Расположение | Использование |
|--------|--------------|---------------|
| us-east-1 | Северная Вирджиния | Америка |
| eu-west-1 | Ирландия | Европа |
| ap-northeast-1 | Токио | Восточная Азия |
| + 5 других | Глобально | См. документацию |

## 🔧 Конфигурация

### Переменные окружения Tick Broadcaster

```yaml
MAX_LATENCY_MS: 150          # Макс. допустимая задержка
MAX_JITTER_MS: 50            # Макс. допустимый джиттер
WARNING_LATENCY_MS: 100      # Порог предупреждения
WARNING_JITTER_MS: 30        # Джиттер предупреждения
DEFAULT_TICK_MS: 100         # Интервал тиков
COUNTDOWN_MS: 3000           # Обратный отсчёт до старта
```

### Переменные окружения Edge Gateway

```yaml
TICK_EU_HTTP: https://xxx.execute-api.eu-west-1.amazonaws.com/prod
TICK_EU_WS: wss://xxx.execute-api.eu-west-1.amazonaws.com/prod
TICK_US_HTTP: https://xxx.execute-api.us-east-1.amazonaws.com/prod
TICK_US_WS: wss://xxx.execute-api.us-east-1.amazonaws.com/prod
USE_TICK_BROADCASTER: true
```

## 📡 Протокол WebSocket

### Клиент → Сервер

```typescript
// Присоединиться к сессии
{ "action": "join", "sessionId": "abc-123", "userId": "user_123" }

// Ping для измерения задержки
{ "action": "ping", "clientTimestamp": 1703123456789 }
```

### Сервер → Клиент

```typescript
// Игровой тик (транслируется каждые 100мс)
{
  "type": "tick",
  "step": 150,
  "value": 42,
  "round": 2,
  "broken": false,
  "serverTimestamp": 1703123456789
}

// Игрок отключён
{
  "type": "kicked",
  "reason": "Средняя задержка 180мс превышает максимум 150мс",
  "avgLatency": 180,
  "maxLatency": 150
}
```

## 🧪 Тестирование

```bash
# Тестирование tick broadcaster
cd tick-broadcaster
npm test

# Тестирование Go бэкенда
go test ./internal/service/...

# Тестирование frontend
cd frontend
npm test
```

## 📖 Документация

- [Полная архитектура](ARCHITECTURE.ru.md) - Дизайн системы с диаграммами
- [Tick Broadcaster](tick-broadcaster/README.ru.md) - Документация Lambda микробэкенда
- [Deterministic Backend](deterministic-backend/README.ru.md) - Документация Go бэкенда
- [Frontend](frontend/README.md) - Документация React приложения

## 🔄 Миграция с клиентских тиков

Предыдущая архитектура вычисляла тики на стороне клиента. Новая архитектура:

| Аспект | Старое (клиент) | Новое (сервер) |
|--------|-----------------|----------------|
| Место тиков | Браузер | AWS Lambda |
| Метод синхронизации | По времени | WebSocket трансляция |
| Читы | Возможны | Предотвращены |
| Чувствительность к задержке | Нет | Да (пороги) |
| Честная игра | Система чести | Принудительная |

## 📈 Будущие улучшения

- [ ] Provisioned Concurrency (устранение холодных стартов)
- [ ] Глобальные таблицы DynamoDB
- [ ] Адаптивная частота тиков
- [ ] Группировка игроков по задержке
- [ ] Аналитический конвейер (Kinesis)
- [ ] Система повторов игр

## 📜 Лицензия

MIT
